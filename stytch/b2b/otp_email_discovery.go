package b2b

// !!!
// WARNING: This file is autogenerated
// Only modify code within MANUAL() sections
// or your changes may be overwritten later!
// !!!

import (
	"context"
	"encoding/json"

	"github.com/stytchauth/stytch-go/v15/stytch"
	"github.com/stytchauth/stytch-go/v15/stytch/b2b/otp/email/discovery"
	"github.com/stytchauth/stytch-go/v15/stytch/stytcherror"
)

type OTPsEmailDiscoveryClient struct {
	C stytch.Client
}

func NewOTPsEmailDiscoveryClient(c stytch.Client) *OTPsEmailDiscoveryClient {
	return &OTPsEmailDiscoveryClient{
		C: c,
	}
}

// Send a discovery OTP to an email address. The OTP is valid for 10 minutes. Only the most recently sent
// OTP is valid: when an OTP is sent, all OTPs previously sent to the same email address are invalidated,
// even if unused or unexpired.
func (c *OTPsEmailDiscoveryClient) Send(
	ctx context.Context,
	body *discovery.SendParams,
) (*discovery.SendResponse, error) {
	var jsonBody []byte
	var err error
	if body != nil {
		jsonBody, err = json.Marshal(body)
		if err != nil {
			return nil, stytcherror.NewClientLibraryError("error marshaling request body")
		}
	}

	headers := make(map[string][]string)

	var retVal discovery.SendResponse
	err = c.C.NewRequest(
		ctx,
		"POST",
		"/v1/b2b/otps/email/discovery/send",
		nil,
		jsonBody,
		&retVal,
		headers,
	)
	return &retVal, err
}

// Authenticate: Authenticates the OTP and returns an intermediate session token. Intermediate session
// tokens can be used for various Discovery login flows and are valid for 10 minutes.
func (c *OTPsEmailDiscoveryClient) Authenticate(
	ctx context.Context,
	body *discovery.AuthenticateParams,
) (*discovery.AuthenticateResponse, error) {
	var jsonBody []byte
	var err error
	if body != nil {
		jsonBody, err = json.Marshal(body)
		if err != nil {
			return nil, stytcherror.NewClientLibraryError("error marshaling request body")
		}
	}

	headers := make(map[string][]string)

	var retVal discovery.AuthenticateResponse
	err = c.C.NewRequest(
		ctx,
		"POST",
		"/v1/b2b/otps/email/discovery/authenticate",
		nil,
		jsonBody,
		&retVal,
		headers,
	)
	return &retVal, err
}
